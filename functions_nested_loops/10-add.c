#include "main.h"
#include <stdio.h>

/**
* add - return the add of two numbers
* @i: first integer
* @n: second integer
*
* Return: value of add
*/

int add(int i, int n)
{
int result;

result = i + n;
return (result);
}
