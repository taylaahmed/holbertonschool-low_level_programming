#include "main.h"

/**
 * _islower - prints based on ASCII numbers
 * @c: character to check
 *
 * Return: Nothing
 */

int _islower(int c)
{

if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
